# Runtime dependencies for the API.
#
# Vercel's Python runtime installs the serverless function's dependencies
# from this file, at the repository root, so it must contain the API's
# dependencies and nothing heavier.
#
# Keep in sync with [project].dependencies + the [api] extra in
# pyproject.toml.
sqlmodel==0.0.37
psycopg[binary]==3.3.4
alembic==1.19.1
fastapi==0.141.1
