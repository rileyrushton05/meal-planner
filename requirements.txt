# Runtime dependencies for Streamlit Community Cloud, which installs from
# this file and then runs ui/streamlit_app.py directly, without installing
# the project as a package.
#
# Keep in sync with [project].dependencies in pyproject.toml, which is what
# local development and CI install from.
streamlit==1.55.0
sqlmodel==0.0.37
streamlit-searchbox==0.1.24
