# Streamlit Community Cloud installs from this file. Dependency versions
# live in pyproject.toml; installing the project itself pulls them in.
#
# Editable (-e) on purpose: app/db.py locates data/ relative to the package
# on disk, so the package must stay in the repo checkout rather than being
# copied into site-packages.
-e .
