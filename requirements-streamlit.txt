# The legacy Streamlit UI, kept working until the React app replaces it.
# Streamlit Community Cloud reads requirements.txt by default, so that
# deployment needs this file selected explicitly (Advanced settings) or it
# will no longer install Streamlit.
-r requirements.txt
streamlit==1.55.0
streamlit-searchbox==0.1.24
